% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_support_functions.R
\name{qc_save_all_plots_png}
\alias{qc_save_all_plots_png}
\title{Save one tall PNG of QC plots for all flagged variables}
\usage{
qc_save_all_plots_png(
  data,
  outfile = "exports/qc_checks.png",
  width_in = 25,
  per_var_in = 2.5,
  dpi = 200
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} containing time series and \verb{*_qcflag} columns.}

\item{outfile}{Path to the PNG file to write (directories are created if needed).}

\item{width_in}{Image width in inches (default \code{25}).}

\item{per_var_in}{Image height per variable (in inches), stacked vertically (default \code{2.5}).}

\item{dpi}{Raster resolution in dots-per-inch (default \code{200}).}
}
\value{
Invisibly returns \code{outfile} (the path written).
}
\description{
Stacks \code{\link[=qc_check_plot]{qc_check_plot()}} for \strong{every} QC variable into a single tall PNG,
allocating a fixed height per variable. Useful for static review or archiving.
}
\details{
Variables are discovered from \code{attr(data, "qc_vars")} when present; otherwise
they are inferred by stripping \code{attr(data, "qc_suffix")} (default \code{"_qcflag"})
from column names that end with that suffix.

Very tall images can exceed some viewers’ limits. If the computed
height exceeds ~30,000 px, a warning is issued; reduce \code{dpi} or \code{per_var_in}
(or export in chunks) if needed.
}
\examples{
\dontrun{
# Minimal demo
d <- data.frame(
  DateTime = seq.POSIXt(Sys.time(), length.out = 1000, by = "hour"),
  temp = rnorm(1000)
)
d <- qc_add_flags(d, vars = "temp")
qc_save_all_plots_png(
  d,
  outfile = "exports/demo_checks.png",
  width_in = 20, per_var_in = 2, dpi = 200
)
}
}
\seealso{
\code{\link[=qc_check_plot]{qc_check_plot()}}, \code{\link[=qc_add_flags]{qc_add_flags()}}, \code{\link[=qc_write_exports]{qc_write_exports()}}
}
