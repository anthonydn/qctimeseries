% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_viewer.R
\name{ts_viewer}
\alias{ts_viewer}
\title{Lightweight Time-Series Viewer}
\usage{
ts_viewer(
  dat,
  y_col,
  win_hrs = 168,
  time_col = "DateTime",
  tz_user = "America/Denver",
  main_height = 440,
  sec_height = 200
)
}
\arguments{
\item{dat}{A \code{data.frame} or \code{data.table} containing at least a POSIXct
time column and one numeric column to view.}

\item{y_col}{\code{string}. Name of the numeric column to display.}

\item{win_hrs}{Window width in hours (default 168 = one week).}

\item{time_col}{Name of the POSIXct column. Defaults to \code{"DateTime"}.}

\item{tz_user}{Time zone used for display (affects x-axis labels only).}

\item{main_height}{Initial height (in pixels) for the main time-series plot.
Sets the starting size of the top plot; can be adjusted in-app via the
“Main height (px)” control. Default is \code{440}.}

\item{sec_height}{Initial height (in pixels) for the secondary plot.
Sets the starting size of the bottom plot; can be adjusted in-app via the
“Secondary height (px)” control. Default is \code{200}. Reduce down to a
minimum of 10 to effectively hide the secondary plot.}
}
\value{
Invisibly returns \code{NULL}. Called for its side-effect of launching
the viewer.
}
\description{
Launch a Shiny app to interactively browse a large numeric time-series.
Data are split into fixed-width windows (e.g. 168 hours) so only one slice
is rendered at a time, allowing smooth interaction even with large datasets.
This viewer is read-only - it does not require or modify any QC flag columns.
}
\examples{
if (interactive()) {
  dummy <- data.frame(
    DateTime = seq.POSIXt(Sys.time(), length.out = 5000, by = "hour"),
    temp     = rnorm(5000, 20, 2)
  )
  ts_viewer(dummy, y_col = "temp")
}
}
