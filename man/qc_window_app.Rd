% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_window_app.R
\name{qc_window_app}
\alias{qc_window_app}
\title{Interactive QC for Time-Series Windows}
\usage{
qc_window_app(
  dat,
  y_col,
  win_hrs = 168,
  qc_suffix = "_qcflag",
  time_col = "DateTime",
  tz_user = "America/Denver"
)
}
\arguments{
\item{dat}{A \code{data.frame} or \code{data.table} containing at least a POSIXct
time column and one numeric column to QC.}

\item{y_col}{\code{string}. Name of the numeric column to display and flag.}

\item{win_hrs}{Window width in hours (default 168 = one week).}

\item{qc_suffix}{Suffix for the flag column. A new integer column
\code{paste0(y_col, qc_suffix)} must already exist; it will be updated in
place (\code{1 = approved}, \code{0 = unchecked}, \code{-2 = flagged}, \verb{-1 = original NA}).}

\item{time_col}{Name of the POSIXct column. Defaults to \code{"DateTime"}.}

\item{tz_user}{Time zone used for display (affects x-axis labels only).}
}
\value{
A \code{data.frame} identical to \code{dat} but with modified QC flag columns.

A \code{data.frame} identical to \code{dat} but with modified QC flag columns.
}
\description{
Launch a Shiny app to visually inspect and flag bad data points in large
numeric time-series datasets.  Data are split into fixed-width windows
(e.g. 168 hours) so only one slice is rendered at a time, allowing smooth
interaction even with millions of rows.
}
\examples{
if (interactive()) {
  dummy <- data.frame(
    DateTime = seq.POSIXt(Sys.time(), length.out = 5000, by = "hour"),
    temp     = rnorm(5000, 20, 2),
    temp_qcflag = integer(5000)
  )
  qc_window_app(dummy, y_col = "temp")
}
Interactive QC for Time-Series Windows

Launch a Shiny app to visually inspect and flag bad data points in large
numeric time-series datasets.  Data are split into fixed-width windows
(e.g. 168 hours) so only one slice is rendered at a time, allowing smooth
interaction even with millions of rows.

if (interactive()) {
  dummy <- data.frame(
    DateTime = seq.POSIXt(Sys.time(), length.out = 5000, by = "hour"),
    temp     = rnorm(5000, 20, 2),
    temp_qcflag = integer(5000)
  )
  qc_window_app(dummy, y_col = "temp")
}
}
\seealso{
For a step-by-step guide, see the package vignette:
\code{vignette("overall_workflow", package = "qctimeseries")}.
Browse all vignettes with \code{browseVignettes("qctimeseries")}.

For a step-by-step guide, see the package vignette:
\code{vignette("overall_workflow", package = "qctimeseries")}.
Browse all vignettes with \code{browseVignettes("qctimeseries")}.
}
